---
description: ai/custom-slash-commandsディレクトリを元に、各種AIごとのスラッシュコマンドを生成する。
---

各種AIツールで同様のスラッシュコマンドを利用できるようにするため、 `ai/custom-slash-commands` ディレクトリを参照し、以下のディレクトリに同じファイル名が存在しない場合はファイルを生成して。
- .claude
- .github/prompts

その際、生成したファイルの中身は生成元へのファイルのパーマリンク（先頭が@のもの）のみを記載して。
